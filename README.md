# ИИ агент для поиска релевантных вакансий

Минимальный прототип интеллектуального агента для поиска вакансий на hh.ru с ранжированием вакансий под пользователя.

---

## 🎯 Цель проекта

Создать систему, которая позволяет пользователю быстро находить релевантные вакансии и отправлять отклики с автоматически сгенерированными сопроводительными письмами.

---

## 📄 Описание проекта

Проект реализован на **FastAPI** с асинхронной работой с базой данных PostgreSQL через **SQLAlchemy (asyncio)**.  

Используются:

- **Docker**: вся инфраструктура может быть поднята через контейнеры, включая PostgreSQL и приложение.
- **Makefile**: содержит команды для linting, форматирования кода, запуска тестов и сборки Docker-образа.
- **GitHub CI/CD workflows**: автоматическая проверка кода при push и pull request, запуск тестов и линтеров.
- **Тесты**: используются **pytest**, **pytest-asyncio** и **Testcontainers[PostgreSQL]** для интеграционных тестов с БД.
- **Слоистая архитектура**: код разделён на слои — **routers**, **services**, **repositories**, **schemas**, **facades**, что упрощает поддержку и тестирование.
- **Паттерн фасад**: фасады (`facades/hh.py`, `facades/chatgpt.py`) скрывают сложность работы с внешними API, предоставляя простой интерфейс для сервисов.
- **SOLID, KISS, DRY**: проект структурирован по принципам объектно-ориентированного проектирования, соблюдая простоту, переиспользуемость и минимизацию дублирования.
- Асинхронная архитектура для быстрого отклика и высокой производительности.
- JWT-аутентификация для безопасного доступа к API.

---

## 📝 Задачи проекта

- Хранение информации о пользователях и их опыте.
- Сбор вакансий с hh.ru (название, описание, URL).
- Генерация сопроводительных писем с помощью ChatGPT.
- Ранжирование вакансий под пользователя с использованием API hh.ru.

---

## 🌐 REST API

| №  | Описание                                 | Endpoint                                                   | Method | Request Body / Params | Response | Authentication |
|----|-----------------------------------------|------------------------------------------------------------|--------|---------------------|----------|----------------|
| 1  | Регистрация нового пользователя         | `/api/v1/auth/register`                                    | POST   | `{ "name": "...", "email": "...", "password": "..." }` | `{ "user_uuid": "..." }` | ❌ |
| 2  | Вход пользователя                        | `/api/v1/auth/login`                                              | POST   | `{ "email": "...", "password": "..." }` | `{ "access_token": "..." }` | ❌ |
| 3  | Получение профиля пользователя           | `/api/v1/users/{user_uuid}/profile`                               | GET    | —                   | `{ "experience": "..." }` | ✅ |
| 4  | Сохранение профиля пользователя          | `/api/v1/users/{user_uuid}/profile`                               | POST   | `{ "experience": "..." }` | `{ "success": true }` | ✅ |
| 5  | Получение списка вакансий с ранжированием | `/api/v1/users/{user_uuid}/ranked_vacancies`                      | GET    | `?query=...&limit=10` | `[ { "vacancy_uuid": "...", "title": "...", "url": "..." } ]` | ✅ |
| 6  | Генерация сопроводительного письма       | `/api/v1/users/{user_uuid}/vacancies/{vacancy_uuid}/cover_letter` | POST   | `{ }`               | `{ "cover_letter": "..." }` | ✅ |
| 7  | Отправка отклика на вакансию с письмом   | `/api/v1/users/{user_uuid}/vacancies/{vacancy_uuid}/responses`    | POST   | `{ "cover_letter": "..." }` | `{ "success": true, "response_uuid": "..." }` | ✅ |

---

## 🗂 Структура базы данных

Минимальный набор таблиц для MVP:

| Таблица                  | Описание |
|---------------------------|----------|
| `users`                  | Информация о пользователях |
| `user_profiles`          | Опыт пользователя (`experience`) |
| `vacancies`              | Вакансии с hh.ru (название, описание, URL) |
| `responses`              | Отклики пользователей с сопроводительными письмами |

### ER-диаграмма базы данных

![DB Diagram](docs/db.svg)

---

## 🛠 Технологии

| Технология                 | Назначение                                           |
|-----------------------------|----------------------------------------------------|
| Python 3.11                 | Основной язык программирования                     |
| FastAPI                     | Веб-фреймворк для REST API                          |
| Uvicorn                     | ASGI сервер для запуска FastAPI                    |
| SQLAlchemy [asyncio]        | ORM для асинхронной работы с PostgreSQL           |
| AsyncPG                     | Асинхронный драйвер для PostgreSQL                |
| Testcontainers[PostgreSQL]  | Тестирование с контейнеризированной БД            |
| Pydantic                    | Валидация данных и схемы                          |
| Pydantic-settings           | Загрузка конфигурации из `.env`                   |
| Python-JOSE / PyJWT         | Работа с JWT токенами                              |
| bcrypt                      | Хэширование паролей                                |
| email-validator             | Валидация email                                   |
| HTTPX                        | Асинхронные HTTP-запросы к внешним API           |
| pytest / pytest-asyncio     | Тестирование кода, поддержка асинхронных тестов  |
| pytest-mock                 | Работа с моками в тестах                           |
| Black                       | Автоформатирование кода                            |
| isort                       | Сортировка импортов                                |
| Flake8                      | Линтинг кода                                       |
| mypy                        | Статическая проверка типов                         |
| autoflake                   | Удаление неиспользуемого кода                      |


## 🌐 REST API

| №  | Описание                                 | Endpoint                                | Method | Request Body / Params                       | Response | Authentication |
|----|-----------------------------------------|----------------------------------------|--------|--------------------------------------------|----------|----------------|
| 1  | Регистрация нового пользователя         | `/api/v1/users`                         | POST   | `{ "name": "...", "email": "...", "password": "..." }` | `{ "user_uuid": "..." }` | ❌ |
| 2  | Вход пользователя                        | `/api/v1/auth/login`                    | POST   | `{ "email": "...", "password": "..." }`   | `{ "access_token": "..." }` | ❌ |
| 3  | Получение информации о пользователе      | `/api/v1/users/{user_uuid}`             | GET    | —                                          | `{ "name": "...", "email": "...", "experience": "..." }` | ✅ |
| 4  | Обновление профиля пользователя          | `/api/v1/users/{user_uuid}`             | PATCH  | `{ "experience": "..." }`                 | `{ "success": true }` | ✅ |
| 5  | Получение списка вакансий с ранжированием | `/api/v1/users/{user_uuid}/vacancies`   | GET    | `?query=...&limit=10`                      | `[ { "vacancy_uuid": "...", "title": "...", "url": "..." } ]` | ✅ |
| 6  | Генерация сопроводительного письма       | `/api/v1/responses`                      | POST   | `{ "user_uuid": "...", "vacancy_uuid": "..." }` | `{ "cover_letter": "..." }` | ✅ |
| 7  | Отправка отклика на вакансию с письмом   | `/api/v1/responses`                      | POST   | `{ "user_uuid": "...", "vacancy_uuid": "...", "cover_letter": "..." }` | `{ "success": true, "response_uuid": "..." }` | ✅ |

---

## 🗂 Структура проекта

```text
.
├── app                 # Основной модуль приложения FastAPI
│   ├── app.py          # Главный модуль и фабрика FastAPI приложения
│   ├── __init__.py     # Делает каталог Python-пакетом
│   └── test_app.py     # Тесты для app.py
├── docs                # Документация проекта
│   ├── db.puml         # PlantUML диаграмма базы данных
│   └── db.svg          # SVG версия диаграммы базы данных
├── engines             # Модули для работы с базой данных и движками
│   ├── db.py           # Логика подключения к БД, контекст сессий
│   ├── __init__.py     # Делает каталог Python-пакетом
│   └── test_db.py      # Тесты для db.py
├── example.env         # Пример файла окружения с настройками
├── facades             # Фасады внешних API (HH, ChatGPT)
│   ├── chatgpt.py      # Работа с ChatGPT API
│   └── hh.py           # Работа с hh.ru API
├── main.py             # Точка входа приложения (запуск сервера)
├── Makefile            # Makefile для управления задачами (lint, test, install)
├── migrations          # SQL миграции для инициализации базы данных
│   ├── 001_create_users_table.sql         # Создание таблицы users
│   ├── 002_create_user_profiles_table.sql # Создание таблицы user_profiles
│   ├── 003_create_vacancies_table.sql     # Создание таблицы vacancies
│   ├── 004_create_responses_table.sql     # Создание таблицы responses
│   └── 005_create_hh_tokens_table.sql     # Создание таблицы hh_tokens
├── poetry.lock         # Блокировка версий зависимостей Poetry
├── poetry.toml         # Настройки Poetry (локальные конфиги)
├── pyproject.toml      # Основной конфигурационный файл проекта Python
├── README.md           # Основная документация проекта
├── repositories        # Репозитории для работы с БД (CRUD)
│   ├── __init__.py     # Делает каталог Python-пакетом
│   ├── models          # SQLAlchemy модели данных
│   │   ├── base.py     # Базовая модель для всех таблиц
│   │   ├── __init__.py # Делает каталог Python-пакетом
│   │   └── user.py     # Модель пользователя
│   ├── test_user.py    # Тесты для репозиториев пользователей
│   └── user.py         # Репозиторий для работы с таблицей users
├── routers             # FastAPI роутеры (эндпоинты)
│   ├── auth.py         # Роутер для авторизации и регистрации
│   ├── __init__.py     # Делает каталог Python-пакетом
│   └── test_auth.py    # Тесты для роутера auth
├── schemas             # Pydantic схемы для валидации данных
│   ├── auth.py         # Схемы для авторизации и регистрации
│   └── __init__.py     # Делает каталог Python-пакетом
└── services            # Сервисы (бизнес-логика)
    ├── auth.py         # Сервис аутентификации (логика регистрации/логина)
    ├── __init__.py     # Делает каталог Python-пакетом
    └── test_auth.py    # Тесты для сервиса auth

```
---

## 👤 Пользовательский сценарий

1. Регистрация и аутентификация пользователей.  
2. Создание профиля с опытом работы.  
3. Сбор вакансий с hh.ru и их хранение.  
4. Генерация сопроводительных писем с ChatGPT.  
5. Ранжирование вакансий через API hh.ru.  
6. Отправка откликов на вакансии через REST API.


## 🚀 Инструкция по развертыванию

1. запустить докер контейнер с бд(postgres)

```text
docker run -d \
  --name ai_job_agent_db \
  -e POSTGRES_DB=ai_job_agent \
  -e POSTGRES_USER=ai_job_agent_user \
  -e POSTGRES_PASSWORD=ai_job_agent_password \
  -p 5432:5432 \
  -v $(pwd)/migrations:/docker-entrypoint-initdb.d \
  postgres:15
```
2. запустить сервер 

```text
python main.py --env-file=.env
```